%section.qa-dashboard
  %h1 QA Dashboard

  / --- Audits Section ---
  %section.audits
    %h2 AUDITS
    %table
      %thead
        %tr
          %th Audit ID
          %th Auditee
          %th Vendor
          %th Type
          %th Start Date
          %th End Date
          %th Risk Level
      %tbody
        - @audits.each do |audit|
          %tr
            %td= audit.id
            %td= audit.auditee
            %td= audit.vendor
            %td= audit.audit_type
            %td= audit.start_date
            %td= audit.end_date
            %td= audit.risk_level

  / --- Compliance Score Section ---
  %section.compliance-scores
    %h2 Compliance Score Overview
    %p YOUR COMPLIANCE SCORE: 67%
    %p ORGANISATION-WIDE AVERAGE: 78%
    / Charts could be implemented here with Chart.js or similar

  / --- Recurring Issues Section ---
  %section.recurring-issues
    %h2 Recurring Compliance Issues
    %table
      %thead
        %tr
          %th Action ID
          %th Description
          %th Vendor
        %tbody
          - @issues.each do |issue|
            %tr
              %td= issue.id
              %td= issue.description
              %td= issue.vendor

  / --- Calendar Section ---
  %section.calendar
    %h2 AUDITS CALENDAR
    / Placeholder for calendar widget (FullCalendar.js or similar)

  / --- Footer / QA Manager Info ---
  %footer
    
